name = "onshape-oauth-proxy"
main = "src/index.ts"
compatibility_date = "2025-02-24"
keep_vars = true

# Deployed to workers.dev — no custom domain needed.
# URL: https://onshape-oauth-proxy.<subdomain>.workers.dev
